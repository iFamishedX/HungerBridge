{
  "schemaVersion": 1,
  "id": "hungerbridge",
  "version": "${version}",
  "name": "HungerBridge",
  "description": "HTTP bridge for executing server commands and logging.",
  "authors": ["iFamished"],
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "server": [
      "com.hungerbridge.fabric.HungerBridgeFabric"
    ]
  },
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": ">=1.21"
  }
}
