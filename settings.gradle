rootProject.name = "HungerBridge"

include("common")
include("fabric")
include("paper")

pluginManagement {
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        gradlePluginPortal()
        mavenCentral()
    }
}
